# 望杏成林 wxcl-v2 整改升级方案

> 整理时间：2026-07-30
> 整合自：灵机评估 + Claude Code 分析

---

## 一、问题汇总

### 1.1 严重问题（必须立即处理）

| 序号 | 问题 | 影响 | 紧迫度 |
|------|------|------|--------|
| S1 | `.env.local` 敏感信息可能泄露 | API Key、数据库密码暴露 | 🔴 紧急 |
| S2 | `node_modules` 可能被提交 | 版本库膨胀、安全风险 | 🔴 紧急 |
| S3 | TypeScript `7.0.2` (Beta) | 生产环境未知风险 | 🔴 紧急 |
| S4 | API 错误响应格式不统一 | 前端处理困难 | 🟡 高 |

### 1.2 技术债务

| 序号 | 问题 | 影响 | 优先级 |
|------|------|------|--------|
| T1 | DeepSeek API 无重试机制 | 网络波动时直接失败 | 🟡 高 |
| T2 | WebSocket 文件存在但未使用 | 代码冗余、困惑 | 🟡 中 |
| T3 | 数据库连接池未配置 | 高并发隐患 | 🟡 中 |
| T4 | AI 调用逻辑分散（chat + analyze） | 代码重复、维护困难 | 🟡 中 |
| T5 | 压力吐槽 Prompt 过于简单 | 与陪伴记录专业度差距大 | 🟡 高 |
| T6 | 前端状态分散（ChildContext 不完整） | 数据一致性风险 | 🟢 低 |
| T7 | middleware 已废弃 | 未来版本兼容性问题 | 🟢 低 |
| T8 | standalone 构建后 static 目录需手动软链 | 部署复杂、容易出错 | 🟡 中 |
| T9 | HTTPS 代理架构链路长 | 排查问题困难 | 🟢 低 |

### 1.3 产品闭环缺失

| 序号 | 问题 | 影响 | 优先级 |
|------|------|------|--------|
| P1 | 机会窗口未闭环 | 核心价值无法体现 | 🔴 高 |
| P2 | 滋养时刻引导缺失 | 用户不知道可以用 | 🟡 中 |
| P3 | 两套认知体系前端缺失 | 数据库有但用户看不到 | 🟡 中 |
| P4 | 六要素无法直观感知 | 用户无法看到结构变化 | 🟡 中 |
| P5 | 家长成长路径模糊 | 缺少反思记录 | 🟢 低 |
| P6 | 预警系统缺失 | 问题积累无法提醒 | 🟢 低 |

---

## 二、整改计划

### 阶段一：安全与基础修复（P0）

#### S1-S3：立即修复

```bash
# 1. 检查 .gitignore 是否正确
node_modules/
.env.local
.env
*.log

# 2. 如果已泄露，立即轮换 DeepSeek API Key
# 3. 将 TypeScript 降级到 5.x
npm install typescript@^5.0.0
```

#### T4：AI 调用统一化

```typescript
// 新建 src/lib/ai.ts
export async function callAI({
  messages,
  systemPrompt,
  maxTokens = 1000,
  retries = 3,
}: {
  messages: { role: string; content: string }[];
  systemPrompt?: string;
  maxTokens?: number;
  retries?: number;
}): Promise<string> {
  const apiKey = process.env.DEEPSEEK_API_KEY;
  if (!apiKey) throw new Error('API Key 未配置');

  const allMessages = systemPrompt
    ? [{ role: 'system', content: systemPrompt }, ...messages]
    : messages;

  for (let attempt = 0; attempt < retries; attempt++) {
    try {
      const response = await fetch('https://api.deepseek.com/chat/completions', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${apiKey}`,
        },
        body: JSON.stringify({
          model: 'deepseek-chat',
          messages: allMessages,
          max_tokens: maxTokens,
        }),
      });

      if (!response.ok) throw new Error(`API Error: ${response.status}`);
      const data = await response.json();
      return data.choices?.[0]?.message?.content || '';
    } catch (err) {
      if (attempt === retries - 1) throw err;
      await new Promise(r => setTimeout(r, Math.pow(2, attempt) * 1000));
    }
  }
  throw new Error('AI 调用失败');
}
```

#### T1：API 错误格式统一

```typescript
// 新建 src/lib/response.ts
export function apiSuccess<T>(data: T, message = '成功') {
  return NextResponse.json({ code: 0, message, data });
}

export function apiError(message: string, code = 400, status = 400) {
  return NextResponse.json({ code, message }, { status });
}
```

---

### 阶段二：核心闭环（P1）

#### P1：机会窗口追踪闭环

**数据库变更**：
```sql
CREATE TABLE IF NOT EXISTS profile_opportunities (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  child_id UUID NOT NULL REFERENCES children(id) ON DELETE CASCADE,
  dimension VARCHAR(50),
  element VARCHAR(50),
  description TEXT NOT NULL,
  suggestion TEXT,
  status VARCHAR(20) DEFAULT 'open',
  first_appeared_at TIMESTAMP,
  last_appeared_at TIMESTAMP,
  appearance_count INT DEFAULT 1,
  warning_level INT DEFAULT 0,
  source_record_id UUID,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_opportunities_child ON profile_opportunities(child_id);
CREATE INDEX IF NOT EXISTS idx_opportunities_status ON profile_opportunities(status);
```

**API 改造**：
1. `analyze/route.ts` 分析后保存机会窗口到 `profile_opportunities`
2. 新记录分析前查询 `status='open'` 的机会窗口
3. Prompt 中加入历史窗口上下文

**前端改造**：
1. 陪伴记录提交后展示"本次关注方向"
2. 全景报告中展示"持续关注"标记

#### P2：滋养时刻主动引导

**前端交互**：
1. 陪伴记录提交成功后，检测滋养时刻 < 3 条时
2. 弹窗："检测到温暖时刻，是否保存到滋养时刻？"
3. 展示提取结果，用户确认后保存

**入口优化**：
1. 滋养页面增加"从记录提取"快捷按钮
2. 滋养报告生成时提示可一键提取

#### P3：两套认知体系可视化

**前端展示**：
1. 画像页面分区：左边"我的理解"、右边"系统洞察"
2. 颜色区分：家长（主色）vs AI（灰色/半透明）
3. 家长修改后记录 `modifiedBy = 'parent'`
4. 权重可视化：简单百分比或对比图

---

### 阶段三：体验增强（P2）

#### P4（补充）：压力吐槽 Prompt 升级

**问题**：当前压力吐槽 prompt 过于简单，与陪伴记录专业度差距大

**改造位置**：`src/app/api/daily-care/analyze/route.ts` 中的 `SYSTEM_PROMPTS.venting`

**改动**：升级为与陪伴记录同等专业级别的框架，返回结构化内容：
```json
{
  "understanding": "理解描述",
  "analysis": "分析描述",
  "suggestions": ["建议1", "建议2", "建议3"],
  "strengths": ["亮点1", "亮点2"],
  "summary": "一句话总结"
}
```

**前端改造**：`src/app/(tab)/questions/page.tsx` 展示结构化回应

#### P5：六要素雷达图

**技术方案**：
1. `profile-extractor.ts` 增加六要素评分逻辑
2. 前端使用 Recharts 绘制雷达图
3. 支持周对比（双层叠加）
4. 点击要素查看具体事件列表

#### P6：家长反思记录

**数据库变更**：
```sql
CREATE TABLE IF NOT EXISTS parent_reflections (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  child_id UUID REFERENCES children(id) ON DELETE SET NULL,
  content TEXT NOT NULL,
  related_record_id UUID,
  related_opportunity_id UUID,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**前端入口**：
1. 陪伴记录页面底部增加折叠的"家长反思"
2. 可选填写，不强制
3. 影响画像中家长认知权重

#### T2：WebSocket 明确用途

```typescript
// 选项 A：移除（如果确定不用）
// 删除 src/lib/websocket.ts

// 选项 B：实现多轮对话
// 新增 /api/chat/conversation 端点
```

---

### 阶段四：产品深化（P3）

#### P7：预警系统

**触发条件**：
1. 同一机会窗口连续 3 次报告都出现
2. `warning_level` 标记为 1
3. 连续 5 次标记为 2（需深入关注）

**通知方式**：
1. 应用内通知入口（我的页面）
2. 每次查看报告时温和提醒
3. 不主动推送，避免焦虑

#### P8：分享海报增强

**海报模板**：
1. 全景报告版
2. 滋养时刻版
3. 里程碑成就版
4. 周对比版

---

### 阶段五：架构优化（贯穿始终）

#### T8：standalone 构建自动化

**问题**：历史教训（adm.nzyy.cc），每次构建后 static 目录需手动软链

**方案**：在部署脚本中自动处理
```bash
# deploy.sh
npm run build
# 确保 static 目录存在
mkdir -p .next/static
# 复制或链接所需资源
```

#### T9：HTTPS 代理架构简化（长期）

**现状**：用户 → 港服(443) → 乌服(443) → Next.js

**方案 A**：乌服直接对外 HTTPS
- 优点：链路短、排查简单
- 缺点：乌服直接暴露

**方案 B**：简化港服配置
- 保持现状，但记录当前配置
- 添加健康检查端点

**建议**：短期保持现状，长期考虑方案 A

---

## 三、技术债务处理

### 高优先级

| 债务 | 处理方式 | 工时 |
|------|----------|------|
| TypeScript 降级 | `npm install typescript@^5.0.0` | 5 分钟 |
| .gitignore 检查 | 添加 `node_modules/`、`.env*` | 5 分钟 |
| API 错误格式统一 | 新建 `response.ts`，统一使用 | 2 小时 |
| DeepSeek 重试 | 在 `ai.ts` 中实现 | 1 小时 |

### 中优先级

| 债务 | 处理方式 | 工时 |
|------|----------|------|
| WebSocket 清理 | 删除或实现 | 4 小时 |
| 连接池配置 | 在 `db.ts` 中添加 | 1 小时 |
| AI 调用统一 | 抽取 `ai.ts` | 2 小时 |
| standalone 构建 | 写入部署脚本自动处理 | 1 小时 |
| 硬编码字符串 | 提取到 constants | 2 小时 |

### 低优先级

| 债务 | 处理方式 | 工时 |
|------|----------|------|
| 前端状态统一 | 完善 ChildContext | 4 小时 |
| middleware 迁移 | 改用 proxy | 待定 |
| HTTPS 架构优化 | 长期规划，评估方案 | 待定 |

---

## 四、实施路径

```
Week 1-2：安全修复 + 技术债务
├── 修复 .gitignore + TypeScript 降级
├── 统一 API 错误格式
├── 抽取 AI 调用模块 + 重试机制
├── WebSocket 决策（删除/实现）
└── standalone 构建脚本自动化

Week 3-4：核心闭环
├── P1 机会窗口追踪闭环
├── P2 滋养时刻主动引导
└── P3 两套认知体系可视化

Week 5-6：体验增强
├── P4 压力吐槽 Prompt 升级
├── P5 六要素雷达图
├── P6 家长反思记录
└── 硬编码字符串提取

Week 7-8：产品深化
├── P7 预警系统
├── P8 分享海报增强
└── 补充单元测试

贯穿始终：架构优化
├── HTTPS 架构评估
└── middleware → proxy 迁移（如需要）
```

---

## 五、回归测试清单

每次发布前必须验证：

- [ ] 登录/注册流程正常
- [ ] 陪伴记录提交 → AI 回复正常（结构化输出）
- [ ] 压力吐槽提交 → AI 回复正常（结构化输出：理解+分析+建议+亮点+总结）
- [ ] 滋养时刻添加/提取/查看正常
- [ ] 画像查看/编辑正常（双版本展示）
- [ ] 全景报告生成正常
- [ ] 机会窗口在报告中展示
- [ ] 多孩子切换正常
- [ ] 退出登录正常
- [ ] 两套认知体系可视化正常
- [ ] 六要素雷达图展示正常（如已上线）

---

## 六、版本规划

| 版本 | 主要内容 | 目标 |
|------|----------|------|
| v2.1 | 安全修复 + 技术债务（TypeScript、重试、API格式） | 稳定基线 |
| v2.2 | 核心闭环（机会窗口、滋养引导、两套认知体系） | 产品闭环 |
| v2.3 | 体验增强（Prompt升级、雷达图、反思记录） | 用户体验 |
| v2.4 | 产品深化（预警系统、海报增强） | 功能完善 |

---

## 七、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| TypeScript 降级后编译错误 | 低 | 高 | 提前在 dev 环境测试 |
| AI 重试增加响应延迟 | 中 | 低 | 设置合理超时 + 用户感知优化 |
| 机会窗口追踪改变用户行为 | 中 | 中 | 灰度发布 + 监控 |
| 数据库迁移失败 | 低 | 高 | 提前备份 + 准备回滚脚本 |
| standalone 构建资源丢失 | 低 | 高 | 部署脚本自动化 + 备份 |
| HTTPS 代理链路问题排查困难 | 中 | 中 | 添加健康检查 + 详细日志 |

---

## 八、待确认事项（已确认）

| 序号 | 问题 | 确认结论 |
|------|------|----------|
| 1 | 预警通知走什么渠道？ | ✅ 应用内 + 查看报告时提示，不主动推送 |
| 2 | 滋养报告生成频率？ | ✅ 按需生成，最多每周一次 |
| 3 | AI 分析更新周期？ | ✅ 报告生成时自动更新 + 家长手动触发 |
| 4 | 机会窗口预警阈值？ | ✅ 3次轻度关注 / 5次深度关注 |
| 5 | WebSocket？ | ✅ 废弃，删除代码 |

### 仍有待确认

| 序号 | 问题 | 影响模块 |
|------|------|----------|
| 6 | standalone 构建脚本的具体实现方式？ | T8 |
| 7 | HTTPS 架构长期方案（方案A/B）？ | T9 |

---

## 九、完整问题清单对照

### 问题来源对照

| 问题编号 | 问题描述 | 整改方案 | 状态 |
|----------|----------|----------|------|
| S1 | .env.local 敏感信息泄露风险 | .gitignore 修复 | 待处理 |
| S2 | node_modules 可能被提交 | .gitignore 修复 | 待处理 |
| S3 | TypeScript 7.0.2 (Beta) | 降级到 5.x | 待处理 |
| S4 | API 错误响应格式不统一 | 统一 response.ts | 待处理 |
| T1 | DeepSeek API 无重试机制 | ai.ts 重试逻辑 | 待处理 |
| T2 | WebSocket 文件存在但未使用 | 删除 websocket.ts | 待确认 |
| T3 | 数据库连接池未配置 | db.ts 配置 | 待处理 |
| T4 | AI 调用逻辑分散 | 抽取 ai.ts | 待处理 |
| T5 | 压力吐槽 Prompt 过于简单 | P4 Prompt 升级 | 待处理 |
| T6 | 前端状态分散 | 完善 ChildContext | 待处理 |
| T7 | middleware 已废弃 | 改用 proxy | 待定 |
| T8 | standalone 构建需手动处理 | 部署脚本自动化 | 待确认 |
| T9 | HTTPS 代理架构链路长 | 架构优化评估 | 待确认 |
| P1 | 机会窗口未闭环 | P1 机会窗口追踪 | 待处理 |
| P2 | 滋养时刻引导缺失 | P2 滋养引导 | 待处理 |
| P3 | 两套认知体系前端缺失 | P3 可视化 | 待处理 |
| P4 | 六要素无法直观感知 | P5 雷达图 | 待处理 |
| P5 | 家长成长路径模糊 | P6 反思记录 | 待处理 |
| P6 | 预警系统缺失 | P7 预警系统 | 待处理 |
| P7 | 分享海报功能单一 | P8 海报增强 | 待处理 |

**总计**：19 项问题，全部已涵盖在整改方案中。
