# wxcl-v2 升级计划（整合版）

> 基于 2026-07-20 tmux 讨论 + 原部署记录整合

---

## 行为边界（必须遵守）

**端口操作**：
- 遇到端口冲突时，禁止直接用 `fuser -k` 或类似命令杀掉端口进程
- 应先用 `lsof -i :端口号` 确认进程身份和归属
- 不确定端口归属时不要操作

**数据库操作**：
- 禁止自行查询、修改数据库中的用户数据
- 禁止自行创建测试账号或激活码
- 如需查看数据库状态，只能提供 SQL 语句，由用户自行执行

**理由**：用户的账号、数据、激活码属于用户个人资产，未经授权不得操作。

**代码提交前检查**：
- 每次提交代码前，检查是否有已发现但未修复的问题
- 能一次性解决的小问题不要留，分批提交
- 修复 bug 和小优化可以一起提交，不分开多个 commit

---

## 一、核心功能升级

### 1. 压力吐槽 Tab

**定位**：用户倾诉困扰，提交后生成专业回应

| 元素 | 名称 |
|------|------|
| 功能入口 | 💬 压力吐槽 |
| 回复展示 | 压力吐槽回应 |
| 回复中 | 提交中... |

**交互逻辑**：
- 用户输入困扰内容
- 提交后，与"陪伴&观察"一样生成内容
- 不暴露 AI 相关字样，强调内在结构养育专业支持

---

### 2. 滋养时刻（新增核心功能）

**定位**：家长在育儿过程中被孩子滋养到的时刻记录

#### 2.1 滋养时刻定义

```
滋养时刻 = 事实 + 正向感受
```

- **事实**：家长记录被孩子滋养的事件，或系统从陪伴&观察/压力吐槽中提取
- **感受**：家长填写正向感受

#### 2.2 正向感受词（不作为标签，提供指引）

被滋养时的感受：
- 温暖、被爱、被理解、被接纳
- 感动、幸福、满足、安心
- **骄傲、自豪、充满力量**

**感受不提供标签，提供指引**：
- "那一刻，你感到..."
- "回想这个时刻，你的感受是？"
- "孩子做了什么，让你感到滋养？"

#### 2.3 系统与用户的关系

- **系统内部用 AI**：对事实和感受进行提炼、分析
- **不告诉用户**：不向用户暴露 AI 相关字样
- 用户感受：这是专业方法论的支持，不是与机器人对话

#### 2.4 滋养时刻记录结构

```
滋养时刻记录：
├── 事实：（家长写或系统从记录中提取）
└── 感受：
    ├── 系统提炼：（系统生成参考，家长可修改）
    └── 自我创建：（家长自己写）
```

#### 2.5 滋养报告

**定期生成**（不依赖记录数量）：
- 本周滋养（本周滋养时刻汇总）
- 本月滋养
- 本季滋养
- 年度滋养

**报告内容**：
- 对正向感受的确认与加强
- 存的是当时的文字表述

---

### 3. 孩子信息结构

#### 3.1 信息分层

```
第一层：基础信息（注册/添加时必填）
├── 姓名
├── 性别（boy/girl）
├── 出生日期（计算年龄用）
└── 年级（可选，初期不强制）

第二层：特质画像（第一次报告后引导补充）
├── 性格特质：类型 + 具体描述
├── 兴趣爱好：列表
├── 优势强项：列表（AI分析 + 家长确认）
├── 支持方向：列表（家长认领，AI不主动定义"问题"）
└── 核心需求：列表

第三层：画像事件库（日常记录中积累）
├── 事件类型：strength / challenge / milestone / interaction / growth
├── 事实描述：具体事件
├── AI解读：（家长可修改）
└── 来源：手动添加 / 从陪伴记录提取 / 从压力吐槽提取
```

#### 3.2 成长目标

```
成长目标
├── 增强（优先）
│   ├── 已有优势1
│   └── 已有优势2
│
└── 支持方向（适量）
    ├── 成长空间1
    └── 成长空间2
```

**两种成长方向**：
| 类型 | 方向 | 说明 |
|------|------|------|
| 增强 | 发挥优势 | 孩子已经做得好的，持续发挥 |
| 支持 | 补短/成长空间 | 孩子需要支持的方面（家长主动认领） |

#### 3.3 两套认知体系 + 动态权重

```
┌─────────────────────────────────────┐
│           家长认知（显性）            │
│    经过情感滤镜，可修改               │
└─────────────────────────────────────┘
              │ 权重（动态调整）
              ▼
┌─────────────────────────────────────┐
│           AI分析（隐性）              │
│    基于客观事件，内部参考             │
└─────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────┐
│        系统支持策略（综合）           │
│    系统心里的"底牌"                  │
└─────────────────────────────────────┘
```

**动态权重规则**：
- 家长认知 ≈ AI分析 → 权重：高
- 家长认知 < AI分析（淡化）→ 权重：动态降低，但不完全忽略
- 家长完全不承认 → 权重：低，以家长表述为主，AI作为隐性参考

**提醒机制**：
- 积累后提醒，不实时触发
- 最多提醒3次，同一问题不重复
- 家长不承认也不强求
- 系统内部保持自己的判断，用于支持策略

**数据来源**：
| 字段 | 来源 | 说明 |
|------|------|------|
| 性格特质 | onboarding + AI 分析 | 用户回忆中体现的行为模式 |
| 兴趣爱好 | onboarding + 日常记录 | 回忆中的事件 + 记录中提到的事物 |
| 优势强项 | AI 分析 | 从陪伴记录中识别做得好/被夸的事件 |
| 支持方向 | AI 分析 + 家长认领 | 发现但不直接定义"问题" |
| 核心需求 | AI 分析 | 综合画像提炼 |

**核心原则**：
- AI 提取的字段，家长都有最终决定权
- 对于"支持方向"，系统可以发现，但让家长自己来认领
- 不能系统直接下结论："你的孩子有问题"
- 保留 AI 分析的目的：系统做事时更准，不是为了给家长挑错

#### 3.4 画像版本管理

```sql
-- 孩子画像（独立表）
CREATE TABLE IF NOT EXISTS child_profiles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  child_id UUID NOT NULL REFERENCES children(id) ON DELETE CASCADE,
  personality JSONB,         -- { type, details[] }
  interests JSONB,         -- string[]
  strengths JSONB,         -- string[]
  challenges JSONB,        -- string[]
  core_needs JSONB,        -- string[]
  growth_goals JSONB,      -- { enhancements[], supports[] }
  ai_analysis JSONB,      -- AI原始分析（内部参考，家长不可见）
  parent_weight FLOAT,     -- 家长认知权重（动态调整）
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 画像事件库
CREATE TABLE IF NOT EXISTS profile_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  child_id UUID NOT NULL REFERENCES children(id) ON DELETE CASCADE,
  event_type VARCHAR(20),   -- strength/challenge/milestone/interaction/growth
  fact TEXT NOT NULL,      -- 事件描述
  interpretation TEXT,     -- AI解读（家长可修改）
  source VARCHAR(20),       -- manual/accompany/venting
  created_at TIMESTAMP DEFAULT NOW()
);

-- 画像版本历史（用于回溯和分析）
CREATE TABLE IF NOT EXISTS profile_versions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  child_id UUID NOT NULL REFERENCES children(id) ON DELETE CASCADE,
  version INT NOT NULL,
  snapshot JSONB NOT NULL,  -- 完整画像快照
  modified_by VARCHAR(20), -- ai / parent
  modifications JSONB,      -- 修改内容记录
  ai_analysis_at_time JSONB, -- 时点的AI分析
  review_flags JSONB,      -- 检视标记
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### 3.5 采集时机

```
注册
  ↓
只问：孩子姓名、性别、出生日期
  ↓
onboarding（3-10条回忆）
  ↓
生成第一份报告 + 初步画像（AI自动提取）
  ↓
提示家长确认/补充
  ↓
画像确立
  ↓
日常记录 → 画像事件库持续积累
  ↓
画像更新（AI分析 + 家长调整）
```

---

### 4. "我的"页面结构

```
👤 我的

孩子信息
├── 孩子A
│   ├── 基本信息
│   └── 全景报告
├── 孩子B
│   ├── 基本信息
│   └── 全景报告
└── + 添加孩子

💧 我的滋养时刻
├── 滋养记录
│   ├── 滋养事件1（事实 + 感受）
│   ├── 滋养事件2
│   ├── 滋养事件3
│   ├── 滋养事件4
│   ├── 滋养事件5
│   └── 更多（每次增加5条）
└── 滋养报告
    ├── 本周滋养
    ├── 本月滋养
    ├── 本季滋养
    └── 年度滋养

🔥 紧急入口
退出登录
```

---

## 二、实施计划

### 第一阶段：Tab 分离完善
- [x] Tab 分离逻辑
- [x] 压力吐槽交互优化（提交后生成内容）

### 第二阶段：孩子信息收集流程
- [x] 孩子信息数据库调整（新增 child_profiles、profile_events、profile_versions 表）
- [x] 画像数据模型（基础信息 + 特质画像 + 成长目标）
- [x] 画像事件库（strength/challenge/milestone/interaction/growth）
- [x] 画像采集向导（5步简化版）
- [x] 画像 API 完善（CRUD + 版本管理）
- [x] 动态权重机制（家长认知 vs AI分析）
- [x] 画像版本历史（用于回溯）

### 第三阶段：滋养时刻功能
- [x] 滋养时刻数据模型
- [x] 滋养时刻列表页（分页：初始5条，点击"更多"每次+5）
- [x] 添加滋养时刻表单
- [x] 滋养报告生成逻辑
- [x] 从陪伴记录中提取滋养事实（系统能力）

### 第四阶段：综合报告升级
- [x] 周对比（本周 vs 上周）
- [x] 本周行动建议
- [x] 记录里程碑
- [x] 可分享海报

---

## 三、核心流程（完整版）

```
注册
  ↓
只收集：孩子姓名、性别、出生日期
  ↓
onboarding（3-10条回忆）
  ↓
生成初始画像（第一份报告）
  ↓
AI 自动提取初步画像（性格、兴趣、优势、支持方向）
  ↓
提示家长确认/补充（不强制，AI 不直接定义"问题"）
  ↓
画像确立（两套认知体系开始运行）
  ↓
画像事件库持续积累（每日记录中收录）
  ↓
AI 回复时参考画像 → 更个性化建议
  ↓
系统隐性更新 AI 分析 + 家长权重动态调整
  ↓
滋养时刻提取 → 滋养报告
  ↓
定期提醒（如有淡化倾向，点到为止）
```

**两套认知体系运行机制**：
- 家长认知（显性）：家长看到和修改的版本
- AI 分析（隐性）：系统内部参考，不暴露给家长
- 权重调整：根据家长与 AI 分析的差异动态调整
- 提醒机制：积累后提醒，最多3次，不强求承认

---

## 四、功能决策

| 功能 | 决策 |
|------|------|
| 心情打卡 | ❌ 删除 |
| 冲突记录 | ❌ 删除 |
| 一对一陪伴 | ❌ 删除 |
| 反思功能 | ❌ 删除 |
| 综合报告 | ✅ 保留，升级 |
| 紧急入口 | ✅ 保留，放在"我的"Tab内 |
| onboarding | ✅ 保留 |
| 孩子信息收集 | ✅ 重构：分层采集 + 两套认知体系 |
| **滋养时刻** | ✅ 新增核心功能 |

---

## 五、部署信息

### 架构

```
┌─────────────────────────────────────────────────────────┐
│                      用户端（外部）                      │
│                                                        │
│   原版: https://wxcl.nzyy.cc/     ← 不要动            │
│   V2:   https://wxcl.nzyy.cc/v2  ← 当前项目           │
│                                                        │
└─────────────────────────┬───────────────────────────────┘
                          │
                          ▼
              ┌─────────────────────────┐
              │        港服             │
              │   （反向代理/通道）     │
              │                         │
              │  /v2 → 转发到乌服       │
              └────────────┬────────────┘
                           │
                           │ 内部网络
                           ▼
              ┌─────────────────────────┐
              │        乌服             │
              │   /home/pupeng/wxcl-v2 │
              │      Next.js :3006      │
              │      PostgreSQL         │
              └─────────────────────────┘
```

### 服务信息

| 项目 | 值 |
|------|-----|
| 代码位置（生产） | `/home/pupeng/wxcl-v2/`（乌服） |
| 服务端口 | 3006 |
| basePath | `/v2` |
| 数据库（生产） | PostgreSQL wxcl（乌服本地） |
| 原版地址 | https://wxcl.nzyy.cc/（不动） |
| V2 地址 | https://wxcl.nzyy.cc/v2 |

### 开发环境

| 项目 | 值 |
|------|-----|
| 代码位置（开发） | `/home/pupeng/wxcl-v2-dev/`（worktree） |
| 分支 | dev-work |
| 服务端口 | 3008 |
| 数据库（开发） | PostgreSQL wxcl_dev |
| 测试账号 | 13800138000 |

**注意：**
- 生产环境受 systemd 管理，代码更新后 `sudo systemctl restart wxcl-v2.service`
- 开发环境是 worktree 隔离，不影响生产
- 开发/生产数据库完全隔离

### 港服配置

- 港服只做反向代理转发，不跑代码
- V2 所有请求通过港服转发到乌服
- 原版 `/` 和 V2 `/v2` 是两个独立应用

### 重要更新（2026-07-27）

**问题描述**：
- wxcl.nzyy.cc/v2 突然无法访问
- 乌服 nginx HTTP 80 端口路由异常，本地测试也不通
- HTTPS 443 端口正常

**解决方案**：
修改港服 nginx 配置，将 HTTPS 转发改为直接到乌服 443 端口：

```nginx
# 关键改动：proxy_pass 从 http:// 改为 https://
location /v2 {
    proxy_pass https://8.147.63.208:443;  # 原来是 http://8.147.63.208:80
    ...
}
```

**架构优化**：
```
之前：用户 → 港服(443 HTTPS) → 乌服(80 HTTP) → 3006
之后：用户 → 港服(443 HTTPS) → 乌服(443 HTTPS) → 3006
```

**安全说明**：
- 全程 HTTPS，更安全
- 乌服不直接暴露在公网
- 港服作为防护层

**后续建议**：
- 考虑简化架构：让乌服直接对外 HTTPS
- 或在港服和乌服之间建立内网专线

---

## 六、产品哲学（2026-07-31 补充）

### 核心定位

**产品起源**：为一位母亲解答育儿困扰时产生的灵感

**产品哲学**：基于内在结构养育，帮助家长"看见"孩子

> 不是告诉家长"你有病我来治"，而是让她好奇"这面镜子会照出我的孩子什么样"

### 两个核心功能

| 功能 | 本质 |
|------|------|
| 陪伴&观察 | 记录成长 → 从内在结构养育视角观察（不是记录任务，是觉察工具） |
| 压力吐槽 | 树洞 + 内在结构养育视角的发现 → 育儿认知重构 |

**关键**：不是"解决问题"，而是"重新理解正在发生什么"

### 滋养时刻

不是核心功能，是支撑性功能。帮助家长从"消耗模式"切换到"滋养模式"。

### 内在结构养育 vs AI

**核心是内在结构养育，不是AI**

- LLM是工具，后台调用
- 内在结构养育是视角、方法论、核心价值
- 产品展示时强调"内在结构养育的发现"，不是"AI分析"

### 用户价值

| 层级 | 描述 |
|------|------|
| 功能层 | 陪伴记录、压力吐槽、滋养时刻、全景报告 |
| 体验层 | 被理解、被看见的温暖，从焦虑到平静 |
| 价值层 | 一种新的看待孩子的方式——不是评判，而是观察 |

**核心感受转变**：
- Before：焦虑、迷茫、不知道自己做得对不对
- After：平静、笃定、"我开始理解我的孩子了"

### 产品哲学关键词

- 看见（不是评判）
- 镜子（不是方法）
- 觉察（不是任务）
- 认知重构（不是给解决方案）
- 机会窗口（不是问题）
- 内在结构养育（不是AI）

### 目标用户

- 有育儿困扰的家长
- 带着好奇来的，不是带着"治病"心态来的
- 愿意先从自己改变、愿意被镜子照一照的

### 产品与竞品差异

| | 绝大多数育儿产品 | 望杏成林 |
|--|--------------|--------|
| 定位 | 给你方法，帮你解决问题 | 给你镜子，帮你看见真实 |
| 用户角色 | 学习者（要学方法） | 被陪伴者（要被看见） |
| 核心动作 | 执行、完成、打卡 | 记录、觉察、接纳 |
| 成功标准 | 孩子改变了 | 家长内心平静了 |

### 测试体验中打动人的部分

**压力吐槽的输出**：
- "孩子的沉默不是对你关上门，而是她还没找到钥匙——而你放下焦虑的陪伴，就是那把钥匙的模具。"
- "你忍住没追问，这本身就是内在结构养育的基础——允许孩子有处理情绪的节奏。"

**陪伴&观察的输出**：
- 三维度亮点框架（心神、自我意向、内化客体）的专业感
- "机会窗口"不是问题，是发展方向
- 系统有记忆，追踪成长轨迹
- 收尾的诗意文案风格

**整体打动人的核心**：
- "看见自己才能看见孩子"——那位母亲的案例核心洞察
- 打破防御靠真实，不靠方法
- 记录不是目的，看见才是

### 营销核心

**展示"发现"过程，而不是功能介绍**

```
常见场景（很多孩子都会有的表现）
    ↓
内在结构养育视角看到了什么
    ↓
激发家长好奇心："我的孩子会怎样？"
```

**落地页应该传达**：
- 什么是内在结构养育
- 它能帮你看见什么
- 用这个视角陪伴孩子是什么体验

**不是**：
- "我们有AI分析功能"
- "记录陪伴获得智能反馈"

### 落地页改版（2026-07-31）

**改版前**：功能介绍式，抽象slogan"看见孩子，看见自己"

**改版后**：
- Hero：主题图 + 主Slogan"让我们一起在时光里，望杏成林"
- 场景开场："孩子成绩下滑，沉默不语——你在焦虑什么？"
- 新增"发现展示"区块：常规视角 vs 内在结构养育视角对比
- 核心发现收尾语："孩子的沉默不是对你关上了门，而是她还没找到钥匙"
- 两个核心功能突出展示（陪伴&观察、压力吐槽）
- 滋养时刻作为次要功能单独说明

---

## 七、两个核心功能的角色差异（2026-08-01 补充）

### 陪伴&观察

**角色定位**：育儿顾问、分析者

**功能**：记录日常亲子时刻，从内在结构养育视角帮助家长"看见"孩子

**核心价值**：
- 帮助家长看见孩子行为背后的心理机制
- 看见成长的发展方向（机会窗口）
- 看见五大心理营养要素在孩子身上的体现

**语气**：冷静、专业、有深度

---

### 压力吐槽

**角色定位**：信任对象、倾听者

**功能**：接住家长的情绪和困扰，给予理解和共情，再从内在结构养育视角帮助家长重新理解

**核心价值**：
- 先给予理解和共情（让家长感到被接住）
- 再帮助家长从更高视角重新理解正在发生什么
- 完成一次"认知重构"

**关键**：不是给方法，不是安慰，而是帮助重新理解

---

## 八、五大心理营养要素（2026-08-01 补充）

### 五个并列的要素

| 要素 | 滋养的是什么 | 缺乏时的感受 |
|------|------------|-------------|
| 自主 | 感觉到"我的选择是被允许的" | 被控制感、窒息感 |
| 实事求是 | 真实地面对现实，不逃避也不幻想 | 活在想象里，或不断受挫 |
| 自我负责 | 从"别人为我负责"到"我为自己负责" | 无力感、依赖 |
| 建设性 | 即使在困境中也有"我能找到办法"的信念 | 习得性无助 |
| 同情心 | 能看见他人、理解他人 | 关系中的孤独 |

### 核心洞察

**这五个要素是并列关系，不是层级关系。**

它们就像身体需要的不同类型营养——缺乏任何一种都会出问题。

---

## 九、滋养时刻的设计哲学（2026-08-01 补充）

### 核心洞察

**五大营养要素不仅滋养孩子，也在亲子互动中滋养家长。**

- 孩子被滋养 → 成长
- 家长被滋养 → 有能量继续陪伴

### 滋养时刻的价值

让家长意识到：**养育不仅是付出，也是收获。**

```
Before：我在为孩子付出
After：孩子也在滋养我，我们彼此滋养
```

### 建议的滋养时刻结构

一条滋养时刻记录：
1. **事实**：孩子做了什么
2. **感受**：那一刻你感到被滋养的是什么
3. **发现**（可选）：这次互动中，你观察到了孩子展现的哪个营养要素

### 滋养报告

定期报告可以问：
- 本周/本月，你在哪些时刻被滋养了？
- 孩子展现了哪些营养要素？
- 这些时刻里，你和孩子的关系是什么样的？

---

## 十、Prompt修正记录（2026-08-01）

### 问题

五大心理营养要素被错误地分成"维度方向"和"要素方向"，导致LLM倾向于合并"自主"和"实事求是"。

### 修正

确认五个要素是并列关系，删除了dimension/element的区分，统一要求LLM独立输出。

### 涉及文件

- `src/app/api/daily-care/analyze/route.ts`
- `src/app/api/daily-care/comprehensive/route.ts`
- `src/app/api/daily-care/batch/route.ts`
